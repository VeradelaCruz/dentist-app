server:
  port: 8090

spring:
  application:
    name: auth-service

  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}

  mongodb:
    uri: ${SPRING_MONGODB_URI:mongodb://localhost:27017/doctor_db}
