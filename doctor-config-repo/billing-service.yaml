spring:
  data:
    mongodb:
      uri: mongodb://mongodb:27017/doctor_db
  kafka:
    bootstrap-servers: kafka:9092
  stripe:
    secret-key: ${STRIPE_SECRET_KEY}
    webhook-secret: ${STRIPE_WEBHOOK_SECRET}
